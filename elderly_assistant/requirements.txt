# 依赖列表（版本已锁定为已验证的稳定版本）
# 配置统一为扁平 .env（python-dotenv 读取），不再使用 pyyaml
python-dotenv
pyttsx3==2.90
requests==2.32.3
pinpong
# HuskyLens 摄像头依赖：dfrobot_huskylensv2
# 该包 PyPI 未发布, 不在此处安装;
# 改由 common/install.py 自动从官方仓库下载并落地 (详见 common/install.py::install_dfrobot_huskylensv2)
# 运行依赖 pinpong (已在上文安装)。
#
# ===== 可选：USB 摄像头条码识别（core/barcode.py 的本地解码通路）=====
# 默认扫码走 HuskyLens 板载解码（无需以下依赖）；仅在使用 USB 摄像头
# (SCAN_SOURCE=usb 或 auto 回退) 时需要，缺失时自动降级，不影响主程序运行。
# 系统级原生依赖（main.py 自动 apt 安装）：libzbar0
pyzbar
opencv-python-headless

# ===== 联网优先 TTS：edge-tts（中文神经语音，质量优于本地 espeak）=====
# 运行时需联网；无网或失败时由 pyttsx3 离线兜底。
# 播放 edge-tts 产出的 MP3 需要系统播放器（main.py 自动 apt 安装 mpg123）。
edge-tts
